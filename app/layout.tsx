import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Randy Andrei Andres - Prompt Engineer & Full-Stack Developer",
  description: "Welcome to the portfolio of Randy Andrei Andres, a passionate Prompt Engineer and Full-Stack Developer. Explore my projects, experience, and skills in AI prompt engineering and software development. Let's connect and create innovative solutions together!",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
